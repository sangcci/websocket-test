package tossiltossil.backend;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class WebsocketTestApplicationTests {

	@Test
	void contextLoads() {
		// This test is empty because it is just a test to check if the context loads.
	}
}
